# Allow Ctrl-S in vim
stty -ixon

# Useful commands to shorten things up
function mkcd {
  mkdir $1
  cd $1
}
alias doit='echo sudo $(fc -ln -1) ; sudo $(fc -ln -1)'
alias cd..="cd .."
alias mkx="chmod +x"
alias config='/usr/bin/git --git-dir=$HOME/.cfg/ --work-tree=$HOME'
alias l="ls -la"
alias restart="exec zsh -i"
